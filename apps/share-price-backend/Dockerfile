# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build share-price-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t share-price-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system share-price-backend && \
          adduser --system -G share-price-backend share-price-backend

COPY dist/apps/share-price-backend share-price-backend
RUN chown -R share-price-backend:share-price-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix share-price-backend --omit=dev -f install

CMD [ "node", "share-price-backend" ]
